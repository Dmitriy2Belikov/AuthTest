using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace LibraryTest.NET.Constants
{
    public static class Rules
    {
        public static (string Id, string Name) Nothing = ("", "Никакие");
        public static (string Id, string Name) AllOrganizations = ("", "Любые");
    }
}
